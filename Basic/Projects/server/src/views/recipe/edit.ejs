<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update One Recipe</title>
</head>
<body>
    <h1>Update One Recipe</h1>
    <form action="/recipes/<%= recipe._id %>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input id="title" placeholder="title" name="recipe[title]">
        </div>
        <div>
            <label for="description">Description</label>
            <input id="description" placeholder="description" name="recipe[description]">
        </div>
        <div>
            <label for="image">Image</label>
            <input id="image" placeholder="image" name="recipe[image]">
        </div>
        <div>
            <label for="price">Price</label>
            <input id="price" placeholder="price" name="recipe[price]">
        </div>
        <button>Submit</button>
        <a href="/recipes/<%= recipe._id %>">Return</a>
        <a href="/recipes">Main Page</a>
    </form>
</body>
</html>